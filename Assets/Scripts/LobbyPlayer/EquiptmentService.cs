using System;
using UnityEngine;

public class EquiptmentService
{
    /*public event Action<WeaponItem> OnWeaponChanged;

    private WeaponItem equipped;
    public WeaponItem currentWeapon => equipped;

    
    public void SetEquipped(WeaponItem weapon)
    {
        if (equipped == weapon) return;

        equipped = weapon;
        OnWeaponChanged?.Invoke(weapon);
    }

    public void ClearWeapon()
    {
        if (equipped == null) return;

        equipped = null;
        OnWeaponChanged?.Invoke(null);
    }*/
}
