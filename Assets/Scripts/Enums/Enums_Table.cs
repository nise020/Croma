using UnityEngine;

public static partial class Enums
{
    public enum StageTableType : int
    {
        Id,
        Type,
        Monster_1,
        Stage,
        Book,
        Buff,
        Reward,
        Combination,
        Character,
        Stat,
    }
}
